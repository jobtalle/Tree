export class Modeller {
    root;

    /**
     * Construct a modeller
     * @param {Node} root The root node
     */
    constructor(root) {
        this.root = root;
    }

    /**
     * Make the model
     */
    model() {

    }
}